# video

Maps to `com.streamingapp.video` (Phase 3).

Planned:
- `videoApi.js` — upload, watch/{videoId}, list, user/{id}/videos
- `VideoUpload.js`, `VideoPlayer.js`, `VideoList.js`, `ChannelVideos.js`
