# comment

Maps to `com.streamingapp.comment` (Phase 4).

Planned:
- `commentApi.js` — POST /api/comments, GET /api/comments/{videoId}, POST /api/comments/reply
- `CommentList.js`, `CommentForm.js`, `ReplyThread.js`
